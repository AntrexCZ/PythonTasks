"""
Написать функцию которая возвращает тру или фолс в зависимости от того "правильная" ли на вход пришла строка

На вход должна подаваться строка состоящая из нескольких чисел символа точка с запятой и еще двух чисел

например

3 4 6; 2 4
1 3 8; 3 5
1 1 1 1 2 3 4 5; 2 3
32 12 23 1111; 230 100

"правильной" строкой считаем строку у которой работает правило.
Сумма чисел до точки с запятой разделенная нацело это первое число после точки с запятой, а остаток от деления это второе число

Например если есть строка

3 4 9; 3 4
3 + 4 + 9 = 16

16/3 нацело = 5
остаток от деления = 1
итого строка "не правильная"

1 1 5 4; 2 3

1 + 1 + 5 + 4 = 11
11/4 нацело = 2
остаток = 3

строка "правильная"
"""


# TODO    finish it later
left, right = input().split(";")

numbers_before = sum(list(map(int, left)))
num_a, num_b = list(map(int, right))


if numbers_before % num_a == num_b:
    print("That is True")
else:
    print("That is False")

print(numbers_before)
print(num_a)
print(num_b)

# def is_string_correct(s):
#     # Разделение строки на две части
#     left, right = s.split(";")
#
#     # Преобразование строк в списки чисел
#     numbers_before = list(map(int, left.split()))
#     num_a, num_b = list(map(int, right.split()))
#
#     # Суммирование чисел в первой части
#     sum_before = sum(numbers_before)
#
#     # Сравнение остатка от деления суммы на первое число во второй части со вторым числом
#     return sum_before % num_a == num_b
#
#
# # Пример использования функции
# print(is_string_correct("3 4 9; 3 4"))  # Вернёт False
# print(is_string_correct("1 1 5 4; 2 3"))  # Вернёт True